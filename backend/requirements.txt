# PromptShield engine — installed from the repo root (editable for local dev).
# On Railway/PyPI this becomes a pinned `promptshield==x.y.z` once published.
-e ../
fastapi==0.115.0
uvicorn==0.30.6
python-dotenv==1.0.1
