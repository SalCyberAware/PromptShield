# Web wrapper runtime dependencies.
#
# The promptshield engine is installed SEPARATELY from the repo root
# (`pip install .`) so a fresh production environment works without the
# editable-parent hack. See railway.json for the build command that does both.
fastapi==0.141.1
uvicorn==0.30.6
python-dotenv==1.2.2
