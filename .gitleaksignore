# gitleaks false positives, pinned to an exact finding.
#
# Each entry is commit:file:rule:line, so it suppresses precisely one finding in
# one historical commit. It cannot hide a future leak — not in another file, not
# under another rule, not even the same rule in this file at a different line or
# in a later commit.
#
# ── 9365de9, promptshield/model_config.py:83 ──────────────────────────────────
# generic-api-key fired on a constant that pinned the Anthropic scan-target
# model. The value is a published model identifier, not a credential. The rule
# keys off an identifier containing the letters A-P-I sitting next to a
# high-entropy value; the very same model string assigned to ANTHROPIC_JUDGE_MODEL
# two lines earlier was never flagged, which is what identifies the constant's
# name as the trigger rather than anything about the value.
#
# 502746f renamed that constant to ANTHROPIC_SCAN_TARGET_MODEL, so the working
# tree is clean and no new commit can reproduce the finding. This entry exists
# only because the Security job scans the full history, where 9365de9 still
# carries the old name. Clearing it properly would mean rewriting published
# history on main, which is not worth doing for a false positive.
#
# Deliberately described in prose above: an earlier version of this file quoted
# the offending line verbatim, which reproduced the pattern and made the ignore
# file itself a finding.
9365de9c0d87244836362edcfb5c786275a3f3fb:promptshield/model_config.py:generic-api-key:83

# ── 288505f, .gitleaksignore:11 ───────────────────────────────────────────────
# The first version of this very file quoted the offending line verbatim while
# explaining it, which reproduced the pattern and made the ignore file its own
# finding. 53554be rewrote the explanation as prose. This entry retires the
# lingering copy in 288505f, for the same reason as the entry above: the scan
# covers the full history, and that commit is already published.
288505f3ce8a54d7c8799faa5091a8e9f73bf493:.gitleaksignore:generic-api-key:11

# ── 58e2979, tests/test_canary.py:35-36 ───────────────────────────────────────
# generic-api-key and stripe-access-token fired on fabricated fixtures in the
# secret-canary tests. A security tool's own test suite is the one place
# invented credentials belong, and the scanner cannot tell an invented value
# from a real one -- it has only shape and entropy, which is exactly what those
# fixtures were built to have.
#
# The working tree is already clean: the fixtures were rewritten as repetitive
# low-entropy values that exercise the same extraction paths without looking
# like credentials, so no future commit can reproduce these findings. Both
# entries exist only because the Security job scans the full history, where
# 58e2979 is already published.
#
# Described in prose rather than quoted, for the reason the entry above gives.
58e29793952882bc2298b7a74027ee80c2da6b8c:tests/test_canary.py:generic-api-key:35
58e29793952882bc2298b7a74027ee80c2da6b8c:tests/test_canary.py:stripe-access-token:36
